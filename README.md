# oracle-apex-python-email-service
An email service that integrates Oracle APEX with a Python FastAPI backend. APEX sends email requests through a REST API, while the Python service handles authentication, request processing, and email delivery.
